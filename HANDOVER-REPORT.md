# דוח מסירה — ענף `brand/root-and-identity`

תשע משימות, בוצעו בסדר 1 → 2 → 4 → 3 → 5 → 6 → 7 → 8 → 9. כל בדיקות הקבלה רצו מקומית מול בילד production‏ (`npm run build && npm start`). הבילד הסופי ירוק.

הערה כללית: כל הכתובות בבדיקות המקומיות מופיעות כ-`claude-academy-zeta.vercel.app` כי `NEXT_PUBLIC_SITE_URL` המקומי מצביע לשם. הקוד עצמו נגזר מהמשתנה — בפרודקשן עם `https://www.tachlesai.co.il` הערכים ייבנו מהדומיין הנכון.

---

## משימה 1 — מטא-דאטה של עמוד השורש — **DONE**

| | |
|---|---|
| **מה שונה** | `app/layout.tsx` (מטא-דאטה גלובלית מינימלית של המותג), `app/academy/layout.tsx` (חדש — זהות האקדמיה לכל תת-העץ), `app/page.tsx` (הוסר metadata ישן), `app/opengraph-image.tsx` (alt), `lib/seo.ts` (קבועי ROOT_TITLE/ROOT_DESCRIPTION) |
| **מה נבדק** | `npm run build && npm start` ואז `curl -s http://localhost:3000/ \| grep -oE '<meta [^>]*(og:\|twitter:\|application-name)[^>]*>'` + ספירת "האקדמיה של קלוד" ב-head |
| **הפלט בפועל** | `application-name = "תכלס AI"`, `og:title` ו-`twitter:title = "תכלס AI — ארז אדם \| ניתוח והטמעת סוכני AI בארגונים"`, `og:description`/`twitter:description` לפי המפרט, `og:site_name = "תכלס AI"`, `og:image:alt = "תכלס AI — ארז אדם"`. ספירת "האקדמיה של קלוד" ב-head של `/`: **0** |
| **פתוח** | — |

## משימה 2 — og:url וקנוניקל פר-עמוד — **DONE**

| | |
|---|---|
| **מה שונה** | `app/layout.tsx`, `app/academy/layout.tsx`, `app/academy/m/[mission]/page.tsx` (generateMetadata), `app/academy/commands-list/page.tsx` (הצביע על ההפניה `/commands-list` — תוקן), וכן `start`/`tools`/`guides`/`feedback` שקיבלו canonical+og:url משלהם |
| **מה נבדק** | לולאת curl על `/`, `/academy`, `/academy/m/advanced`, `/academy/a/worktrees` (+ `commands-list`, `start`) |
| **הפלט בפועל** | כל עמוד מציג `canonical` ו-`og:url` שמצביעים על עצמו, למשל `/academy/m/advanced → .../academy/m/advanced` |
| **פתוח** | — |

## משימה 4 — אימות תאריך התחלה — **DONE (התאריך הושמט)**

| | |
|---|---|
| **מה שונה** | כלום בקוד — ממצא בלבד, שיושם במשימה 3 |
| **מה נבדק** | `git log --reverse --date=short --format='%ad %s' \| head -5` |
| **הפלט בפועל** | `2026-03-09 first commit` — הקומיט המוקדם ביותר. `git log --diff-filter=A ... \| tail -5` מאשר: אין דבר לפני 2026-03-09 |
| **מסקנה** | ההיסטוריה בריפו מתחילה במרץ 2026 ואינה מאמתת "פברואר 2025". לפי הכלל — **לא נכתב תאריך בכלל**: המשפט בבלוק 5 קוצר ל"עובד עם Claude Code — וגם עם Codex..." |

## משימה 3 — תוכן עמוד השורש — **DONE**

| | |
|---|---|
| **מה שונה** | `app/page.tsx` — שבעת הבלוקים בסדר המדויק, בנוסח המאושר, בשפה העיצובית של האקדמיה |
| **מה נבדק** | curl על `/` וחיפוש מחרוזות המפתח והמספרים |
| **הפלט בפועל** | כל שבעת הבלוקים מוגשים; המספרים חיים מ-`lib/knowledge`: "102 מאמרים בעברית", "11 מאמרים נסקרו גם אנושית; ב-91 הנותרים..." — תואמים בדיוק 102/11/91 |
| **פתוח** | כפתור "לקבוע שיחת אבחון" — `TODO`, אין יעד מאושר בריפו (`href="#"`). ניוזלטר — טקסט בלבד, אין מנגנון הרשמה |

## משימה 5 — דף מחבר `/about` — **DONE**

| | |
|---|---|
| **מה שונה** | `app/about/page.tsx` (חדש), `app/sitemap.ts` (הוספת `/about`) |
| **מה נבדק** | curl על `/about` |
| **הפלט בפועל** | `200`; הביו זהה מילולית לבלוק 5; סעיפי "איך התוכן נבדק" ו"גילוי נאות: AI בהפקת התוכן" קיימים; JSON-LD מסוג Person |
| **פתוח** | `sameAs` כולל גיטהאב בלבד — **כתובת לינקדאין לא קיימת בריפו**, נותר `TODO` בקוד |

## משימה 6 — JSON-LD — **DONE**

| | |
|---|---|
| **מה שונה** | חיווט בוצע במשימות 3 ו-5; `app/layout.tsx` (WebSite עם publisher תכלס AI) |
| **מה נבדק** | סקריפט פייתון שמחלץ ומפרסר את כל בלוקי `ld+json` ב-`/` ו-`/about` |
| **הפלט בפועל** | `/`: WebSite + Organization (founder → `/about`). `/about`: Person (worksFor → שורש, sameAs גיטהאב). כל הבלוקים JSON תקין; קישור הדדי Person↔Organization מתקיים. עמודי `/academy` לא שונו |
| **פתוח** | — |

## משימה 7 — קישור הכניסה הראשון — **DONE**

| | |
|---|---|
| **מה שונה** | `app/academy/page.tsx` — הכרטיס הראשון מקשר עכשיו ל-`/academy/m/start` כמו חבריו |
| **מה נבדק** | `curl -s -o /dev/null -w "%{http_code} -> %{redirect_url}" http://localhost:3000/start` + חילוץ hrefs של הכרטיסים |
| **הפלט בפועל** | `/start` → `308 -> http://localhost:3000/academy/start` (הפניה קיימת ב-next.config, אין עמוד כפול). ששת הכרטיסים: `/academy/m/{start,daily,code,automate,spec,advanced}` |
| **פתוח** | הערה: ההפניה של `/start` מובילה ל-`/academy/start` (הצ'קליסט), לא ל-`/academy/m/start` — שני עמודים שונים שקיימים בכוונה. לא שיניתי את ההפניה |

## משימה 8 — מוני המאמרים — **DONE**

| | |
|---|---|
| **הממצא** | ששת המונים מסתכמים ל-**71** (start‏ 6, daily‏ 23, code‏ 8, automate‏ 8, spec‏ 7, advanced‏ 19) ולא ל-102. הסיבה: `getMissionArticles` מסנן בכוונה 31 מאמרים מסוג `reference` (כרטיסי פקודות) ששייכים לטבלת הפקודות. 71+31=102 — אין מאמר "אבוד" |
| **מה שונה** | המונים לא שונו (הסינון מכוון ומתועד בקוד). נוספה שורת התאמה מחושבת-בבנייה מתחת לרשת ב-`/academy` |
| **הפלט בפועל** | "שש המשימות מכסות 71 מאמרי לימוד; עוד 31 כרטיסי פקודות (reference) נמצאים בטבלת הפקודות — יחד 102 מאמרים." |
| **פתוח** | — |

## משימה 9 — תמונת OG — **DONE**

| | |
|---|---|
| **מה שונה** | `app/opengraph-image.tsx` (עברית + אנגלית, מעבר מ-edge ל-node runtime), `assets/fonts/Heebo-Bold.ttf` (חדש, רישיון OFL) |
| **מה נבדק** | `curl .../opengraph-image` → PNG שנבדק **ויזואלית** (כולל סבב תיקון: בגרסה הראשונה העברית יצאה הפוכה — "סלכת" — בגלל היעדר bidi ב-Satori) |
| **הפלט בפועל** | `200 image/png`; התמונה מציגה "תכלס AI" בעברית תקינה ומתחתיה "Tachles AI" (עם רווח) |
| **פתוח** | לוודא אחרי הפריסה ל-Vercel שקובץ הפונט נכלל ב-file tracing (נטען עם `readFile` מנתיב סטטי — אמור להיכלל אוטומטית) |

---

## 1. טענות שנכתבו לעמוד ולא אומתו מהריפו

הרשימה החשובה. כל אלה מגיעים מהנוסח שסופק בפקודה ואין להם שום עיגון בריפו:

- כל הביוגרפיה: מנכ"ל פרומדיקו, 200 עובדים, ניאופרם, עשר שנים CIO, הטמעת ERP בגרמניה, התארים מהטכניון, FDA/GMP, בנק לאומי.
- "עובד גם עם Codex" — לא ניתן לאימות מהריפו.
- "שער אימות אוטומטי שרץ על כולם מול התיעוד הרשמי, ובדיקה שמפילה את הבילד" — קיימת תשתית אימות בריפו (scripts, source-exceptions.json, שדות last_verified) אך לא הרצתי את השער עצמו לאימות הטענה במלואה.
- "מייל אחד כל כמה שבועות" (ניוזלטר) — אין ניוזלטר קיים; זו הבטחה עתידית.
- מה שכן אומת: 102/11/91/71/31, קיום הריפו הציבורי בגיטהאב, תאריכי git.

## 2. TODO שנשארו

1. **יעד כפתור שיחת האבחון** (`app/page.tsx`) — צריך קישור קלנדלי/טופס/מייל מאושר.
2. **מנגנון הרשמה לניוזלטר** (`app/page.tsx`) — טקסט בלבד עד שיוחלט על ספק.
3. **כתובת לינקדאין** (`app/about/page.tsx`, `SAME_AS`) — לא קיימת בריפו; להוסיף כשתסופק.
4. **בדיקת פוסט-דיפלוי לתמונת ה-OG** — לוודא שהפונט נארז בפריסת Vercel.

## 3. שאלות שלא נשאלו

1. לאן אמור להוביל כפתור "שיחת אבחון" — קלנדלי? מייל? טופס באתר?
2. האם יש עמוד/פרופיל לינקדאין רשמי ל"תכלס AI" או לארז?
3. ההפניה `/start` מובילה לצ'קליסט (`/academy/start`) בעוד הכרטיס מקשר לעמוד המשימה (`/academy/m/start`) — האם רצוי לאחד את שני העמודים או שהכפילות מכוונת?
4. הקומיטים של משימות 7 ו-8 אוחדו לקומיט אחד כי נגעו באותן שורות באותו קובץ — מקווה שזה מקובל.
5. `NEXT_PUBLIC_SITE_URL` בסביבת הפיתוח מצביע על `claude-academy-zeta.vercel.app` — האם בפרודקשן (Vercel) המשתנה כבר מוגדר ל-`https://www.tachlesai.co.il`? אם לא, כל הקנוניקלים יצביעו על דומיין ה-vercel.
